---
- hosts: localhost
  gather_facts: no

  tasks:
  - name: get info on an image
    gcp_compute_image_info:
      filters:
      - name = "{{ gcp_vm_disk }}"
      auth_kind: serviceaccount
    register: image_info

  - name: Print images 
    debug:
            var: image_info 
